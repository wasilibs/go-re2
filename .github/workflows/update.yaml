name: Update Upstream

on:
  schedule:
    - cron: "5 4 * * *"
  workflow_dispatch:

permissions:
  id-token: write
  attestations: write

jobs:
  build:
    uses: wasilibs/actions/.github/workflows/update.yaml@471602b854607eed6141d224f6fb5b9e1335f7f1
    secrets: inherit
